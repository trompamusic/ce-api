type Mutation {
  RequestControlAction(identifier: ID, controlAction: RequestControlActionInput): ControlAction
  UpdateControlAction(identifier: ID, actionStatus: ActionStatusType!, error: String): ControlAction
}
