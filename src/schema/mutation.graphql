type Mutation {
  RequestControlAction(identifier: ID, controlAction: RequestControlActionInput): ControlAction
  UpdateControlAction(identifier: ID, actionStatus: ControlActionStatus!, error: String): ControlAction
}
